# 작업일지 - 2026-06-29

## 작업 대상

- 프로젝트: `schedule-app`
- 주요 화면: 일별 스케줄 목록
- 주요 파일:
  - `src/components/schedule/TimeSlotRow.tsx`
  - `src/components/schedule/SchedulePage.tsx`
  - `src/components/schedule/CompletionPopup.tsx`
  - `src/components/schedule/DepositPopup.tsx`
  - `src/types/index.ts`

## 1. 일정 목록 내용 행 추가 기능

### 요청

- 일정 목록의 `내용` 입력 박스 오른쪽에 `+` 버튼 추가
- `+` 클릭 시 같은 내용 입력 박스를 아래에 추가
- 추가된 내용 행에는 삭제 버튼 표시
- 추가된 내용도 저장/조회 가능해야 함
- Android WebView 호환을 위해 버튼 스타일은 인라인 스타일 사용

### 처리 내용

- 기존 `schedules.memo` 단일 텍스트 값을 여러 내용 행으로 다룰 수 있도록 변경
- 기존 단일 텍스트 데이터는 첫 번째 내용 행으로 자동 호환
- 내용이 여러 개인 경우 `memo`에 JSON 문자열 배열 형태로 저장
  - 예: `["내용1","내용2"]`
- PC/모바일 일정 행 모두에 내용 추가/삭제 UI 반영
- `+` 버튼은 첫 번째 내용 입력 옆에 표시
- 추가된 행에는 `-` 삭제 버튼 표시
- 입력 중인 내용 행을 추적해서 품목 선택 시 해당 행에 값이 들어가도록 처리

### 관련 보완

- `SchedulePage.tsx`
  - 복사 텍스트에서 JSON 배열이 그대로 보이지 않도록 표시용 변환 추가
- `CompletionPopup.tsx`
  - 완료 확인 팝업의 작업 내용에 JSON 배열이 그대로 보이지 않도록 줄바꿈 표시 변환 추가
- `src/types/index.ts`
  - `memo`가 단일 텍스트 또는 JSON 문자열 배열을 담을 수 있음을 주석으로 명시

## 2. 내용 행별 품목 금액 합산 연동

### 요청

- 첫 번째 내용과 추가된 내용의 금액을 합산해서 일정 금액에 연동

### 처리 내용

- 내용 행 값이 등록된 품목명과 정확히 일치하면 해당 품목 가격을 합산
- 품목 드롭다운에서 선택하면 전체 내용 행의 품목 가격 합계로 `amount` 갱신
- 내용 행 삭제 시 남은 내용 행들의 품목 가격 합계로 `amount` 재계산
- 직접 입력 후 blur 시에도 품목명과 일치하면 합산 반영
- 품목명과 매칭되지 않는 행은 합산에서 제외

## 3. 입금확인 팝업 메모와 목록 내용 분리

### 요청

- 일정 목록에서 입금확인 뱃지 클릭 후 팝업 메모를 입력하면 목록의 내용이 바뀌는 문제 수정
- 입금확인 팝업 메모는 목록 내용과 별도로 저장
- 입금확인 완료 후에도 팝업 메모가 지워지지 않고 남아 있어야 함

### 원인

- 입금확인 완료 처리에서 `deposit_memo`를 스케줄의 `memo`에 저장하고 있었음
- 이 때문에 목록의 `내용` 필드가 입금 메모로 변경됨

### 처리 내용

- 입금확인 메모를 더 이상 `schedules.memo`에 저장하지 않도록 수정
- 입금확인 메모는 `completion_records` 테이블에 별도 기록으로 저장
  - `record_type: 'deposit'`
  - `memo: deposit_memo`
- 완료 확인 기록과 입금 확인 기록을 분리 조회
  - 완료 확인: `record_type === 'completion'`
  - 입금 확인: `record_type === 'deposit'`
- 입금확인 팝업을 다시 열 때 기존 입금 메모를 불러와 표시하도록 수정

## 4. 카드/VAT 입금완료 시 금액 이중 부가세 적용 수정

### 요청

- 일정 목록에서 카드/VAT 선택 시 목록 금액은 부가세 포함 금액으로 정상 표시됨
- 입금확인 팝업에서 입금완료를 누르면 목록 금액에 부가세가 한 번 더 적용되어 올라가는 문제 확인 및 수정

### 원인

- 일정 목록은 저장된 기본 금액에 `1.1`을 곱해 카드/VAT 금액을 표시함
- 입금확인 팝업 완료 시 카드/VAT의 `부가세 포함 금액`을 저장값으로 넘기고 있었음
- 결과적으로 저장 금액 자체가 1.1배가 되고, 목록에서 다시 1.1배 표시되어 이중 적용됨

### 처리 내용

- 입금확인 팝업에서 `부가세 포함 금액`은 표시 용도로만 유지
- 입금완료 시 저장되는 금액은 기본 금액 `amount` 그대로 전달
- 입금확인 팝업을 열 때 현재 일정의 기본 금액을 우선 사용하도록 조정

## 5. APK 빌드

### 수행한 빌드 절차

1. `npm run build`
2. `npx cap sync android`
3. `android/gradlew.bat assembleDebug`
4. 생성된 APK를 프로젝트 루트의 `schedule-app.apk`로 복사

### 최종 APK

- 경로: `C:\work\schedule-app\schedule-app.apk`
- 크기: `5,539,268 bytes`
- 생성 시각: `2026-06-29 오전 11:19:37`
- 원본 산출물:
  - `android/app/build/outputs/apk/debug/app-debug.apk`

## 검증 결과

- `cmd /c npx tsc --noEmit` 통과
- `cmd /c npm run lint` 통과
- `cmd /c npm run build` 통과
- `cmd /c npx cap sync android` 통과
- `cmd /c .\gradlew.bat assembleDebug` 통과

## 참고 사항

- Gradle wrapper 실행 시 네트워크 제한으로 Gradle 배포판 다운로드가 차단되어 승인 후 재실행함
- 최종 빌드는 성공적으로 완료됨
- 현재 APK는 debug 빌드 산출물임
